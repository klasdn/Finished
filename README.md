To use it, first install Django through pip install Django

Go to RSI settings and change the email password and user variables

Bugs: 
Signature is not cleared after hitting submit 
If user press submit on esig more than once, data duplication is possible

Most likely error from JavaScript as I had modified the js to process and accept signatures

Email message is only sending a string of messages, qr code and esig is not
included (nakalimutan ko siya)
